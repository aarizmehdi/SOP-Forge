[build]
builder = "nixpacks"
watchPatterns = ["backend/**"]

[build.nixpacks]
providers = ["python"]

[deploy]
startCommand = "cd backend && pip install -e . && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
